import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import {createClient} from 'jsr:@supabase/supabase-js@2'
Deno.serve(async(req)=>{try{const auth=req.headers.get('Authorization')??'';const supabase=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}});const {data:{user}}=await supabase.auth.getUser();if(!user)return new Response(JSON.stringify({error:'Unauthorized'}),{status:401});const {orderId,otp}=await req.json();if(!orderId||!/^[0-9]{4}$/.test(otp))return new Response(JSON.stringify({error:'Invalid input'}),{status:400});const {data,error}=await supabase.rpc('verify_pickup_otp',{p_order:orderId,p_otp:otp});if(error)return new Response(JSON.stringify({error:error.message}),{status:400});return new Response(JSON.stringify({ok:Boolean(data)}),{status:data?200:403,headers:{'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:'Unknown error'}),{status:500})}})
